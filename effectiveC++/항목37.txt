37: 어떤 함수에 대해서도 상속받은 기본 매개 변수 값은 절대로 재정의하지 말자

 가상함수에 쓰이는 기본매개변수값은 정적객체에서 ->정적 바인딩
 가상함수는 동적객체에서 가져온다 		 ->동적 바인딩

 객체의 정적타입 -> 프로그램 소스 안에 놓는 선언문을 통해 그 객체가 갖는 타입
 객체의 동적타입 -> 그 객체가 진짜로 무엇이냐에 따라 결정되는 타입

 shap* pc = new Circle // 정적타입 shape, 동적타입 circle
